{
    "name": "root",
    "private": false,
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "scripts": {
        "setup": "npm install --no-package-lock && npm run bootstrap",
        "bootstrap": "lerna bootstrap",
        "version": "lerna clean --yes && lerna exec \"npm install --ignore-scripts --package-lock-only\" && git add packages/*/package-lock.json"
    }
}
