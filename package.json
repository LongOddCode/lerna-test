{
    "name": "root",
    "private": false,
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "scripts": {
        "setup": "npm install && npm run bootstrap",
        "bootstrap": "lerna bootstrap"
    },
    "dependencies": {
        "lerna-test-pkg2": "^0.9.17-alpha.0"
    }
}
