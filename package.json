{
    "name": "root",
    "private": false,
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "scripts": {
        "setup": "npm intall --no-package-lock && npm run bootstrap",
        "bootstrap": "lerna bootstrap"
    }
}
