name: test

on:
  workflow_dispatch:
  pull_request:

jobs:
  setup:
    if: github.actor == "deps"
    runs-on: ubuntu-latest
    steps:
      - name: do sth.
        run: |
          echo "do sth."
